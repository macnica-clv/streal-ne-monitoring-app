name: Hiz-mil
channels:
  - conda-forge
dependencies:
  # requirements.txt と同じ制約を維持すること / keep in sync with requirements.txt
  - python=3.12
  - pyside6>=6.5,<7
  - pyqtgraph>=0.13
  - numpy>=1.24,<3
  - pandas>=2.0,<3
  - scipy>=1.10,<2
  - pyserial>=3.5
  # ビルド用 / for building with PyInstaller (requirements-build.txt)
  - pyinstaller>=6.0
