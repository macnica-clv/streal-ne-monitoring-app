# Runtime dependencies for the Hiz-mil GUI application.
#   pip install -r requirements.txt

PySide6>=6.5,<7
pyqtgraph>=0.13
numpy>=1.24,<3
pandas>=2.0,<3
scipy>=1.10,<2
pyserial>=3.5

# Optional: speeds up pandas / chart computations.
# The application runs without them.
# bottleneck>=1.3
# numexpr>=2.8
